<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ config.title|striptags|e }}</title>
    <link rel="alternate" type="text/html" href="{{ home }}" />
    <link rel="self" type="application/atom+xml" href="{{ home }}atom.xml" />
    <id>{{ home }}/</id>
{% for post in posts %}
{% if loop.first %}
    <updated>{{ post.timestamp.strftime('%Y-%m-%dT%H:%I:%SZ') }}</updated>
{% endif %}
<entry>
    <title>{{ post.title|striptags|e }}</title>
    <link rel="alternate" type="text/html" href="{{ home }}/{{ post.url }}" />
    <id>tag:{{ home.split('/')[2] }},{{ post.timestamp.strftime('%Y-%m-%d') }}:{{ post.url }}</id>
    <updated>{{ post.timestamp.strftime('%Y-%m-%dT%H:%I:%SZ') }}</updated>
    <summary type="html">
      {{ post.metadata.summary|e }}
        &lt;p&gt;&lt;a href="{{ home }}/{{ post.url }}"&gt;Permalink&lt;/p&gt;
    </summary>
    <author>
        <name>{{ config.author.name }}</name>
        <email>{{ config.author.email }}</email>
    </author>
    {% for category in sorted(post.categories) %}
    <category term="{{ category }}" />
    {% endfor %}
    <content type="html">
        {{ render(post.content)|e }}
        &lt;p&gt;&lt;a href="{{ home }}/{{ post.url }}"&gt;Permalink&lt;/p&gt;
    </content>
</entry>
{% endfor %}
</feed>

